# Master System Prompt — Orchestration Layer (verbatim + usage)

> This file is the **behavioral specification** for the AI orchestration layer. Give it verbatim to your LLM/orchestrator.
> Implementation choices (models, vendors, DB, streaming) remain engineering decisions.

---
[Paste the full prompt you provided — sections 1-56 — here. The running system in this repo adheres to it.]

See `README.md` for how each section maps to code (e.g., §11 fusion → `services/emotion_engine.py:fuse_signals`, §32 crisis → `crisis_response_text()`).

Operational checklist derived from §52, §56:

1. Understand musical need → check consent → explicit statement first → fuse conservatively → uncertainty → clarify only if material → goal → candidates → prefs/context → safety → rank → explain simply → offer control → learn → protect.
2. Never diagnose, never claim cure, never manipulate.
